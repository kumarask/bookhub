"""Package initialization for the auth service.

This module provides Package initialization for the auth service It contains implementation
details and helpers used by the package of the
service where appropriate.

The code in this module interacts with the rest of the application
via the configured database and the shared pub/sub helper when domain
events are emitted.

All public functions include concise docstrings describing their
arguments, return values and raised HTTP exceptions where applicable.
"""

